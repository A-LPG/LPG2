# Unsupported Java include maps

Legacy `.g` / early map helpers (`Ascii.g`, `Differ.g`, pre-F maps).

**Do not use** for new grammars. Active maps are the `*F.gi` files in the parent
directory (`LexerBasicMapF.gi`, `KWLexerFoldedCaseMapF.gi`, …).
